# Shared Resources

This directory contains code, configuration, databases, utilities, and assets shared across multiple AI agent demos.

## Structure
- `config.py`: Shared environment variable loader and database credentials.
- `db/`: Directory for shared local database engines (SQLite, vector stores, etc.).
- `utils/`: Common utilities (logging helpers, common API client wrappers, schema validators).